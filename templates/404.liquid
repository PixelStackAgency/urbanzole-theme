<div class="not-found">
  <div class="not-found-number">4<span>0</span>4</div>
  <h1>Page Not Found</h1>
  <p>Looks like this page took a wrong turn. Let's get you back on track.</p>
  <div class="not-found-actions">
    <a href="{{ routes.root_url }}" class="btn btn--primary">Back to Home</a>
    <a href="{{ routes.all_products_collection_url }}" class="btn btn--ghost">Shop All</a>
    <a href="{{ routes.search_url }}" class="btn btn--ghost">Search</a>
  </div>
</div>
